// -*- mode: js2 -*-
import { hash } from "kitsune/util";

export default function autoPut({ stringPut, string }) {
    let id = hash(string);
    return stringPut({ element: { id, string } });
}
module.exports = autoPut;
