// -*- mode: js2 -*-
import { createId } from "kitsune/util";

export default function autoPut({ put, element }) {
    if(!element.id) {
        var id = createId(element);
        element.id = id;
    }
    return put({ element });
}
module.exports = autoPut;
