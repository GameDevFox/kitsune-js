// -*- mode: js2 -*-
function load({ path, id }) {
    let systemPath = path+"/"+id;

    let system = null;
    try {
        system = require(systemPath);
    } catch(e) {
        // ignore and return null
    }
    return system;
}
module.exports = load;
