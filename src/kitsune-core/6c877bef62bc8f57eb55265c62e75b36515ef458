// -*- mode: js2 -*-
import { createId } from "kitsune/util";

export default function assign({ graphAutoPut, head, type, tail }) {
    var edge = graphAutoPut({ head, tail });
    return graphAutoPut({ head: type, tail: edge });
}
module.exports = assign;
