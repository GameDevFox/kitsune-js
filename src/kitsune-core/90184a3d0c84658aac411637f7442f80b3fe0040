// -*- mode: js2 -*-
export default function and({ types, node }) {
    var notA = types.find(type => {
        let notA = !type({ node: node });
        return notA;
    });
    return !notA;
}
module.exports = and;
