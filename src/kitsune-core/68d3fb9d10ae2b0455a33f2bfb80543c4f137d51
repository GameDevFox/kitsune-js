// -*- mode: js2 -*-
export default function returnFirst(func) {
    let newFunc = function(input) {
        let output = func(input);

        let result = output[0];
        if(!result)
            throw new Error("output does not have a first element");

        return result;
    };
    return newFunc;
}
module.exports = returnFirst;
