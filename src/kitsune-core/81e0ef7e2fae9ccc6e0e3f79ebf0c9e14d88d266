// -*- mode: js2 -*-
export default function getNames({ graphFactor, stringFind, nodes }) {
    let names = graphFactor({ head: nodes, type: "f1830ba2c84e3c6806d95e74cc2b04d99cd269e0" });
    names.forEach(node => {
        let strResult = stringFind({ where: { id: node.tail } }).map(result => result.string);
        node.name = strResult;
    });
    return names;
}
module.exports = getNames;
